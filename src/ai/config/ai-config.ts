/**
 * @file Centralized AI model and configuration for Genkit flows.
 */

/**
 * Configuration settings for the AI model.
 * - temperature: Controls the randomness of the output.
 */
export const config = {
  temperature: 0.1,
};
